# Hero Lineup Web

`hero-lineup` 的纯 Web 静态迁移项目。

目标运行方式：

```bash
npm ci
npm run build
```

构建生成的 `dist/` 可直接由 Nginx 托管，不依赖 Rust、Tauri、Node.js 服务或后端 API。

完整迁移计划请参阅 [docs/README.md](./docs/README.md)。
